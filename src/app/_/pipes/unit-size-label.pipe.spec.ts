import { UnitSizeLabelPipe } from './unit-size-label.pipe';

describe('UnitSizeLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new UnitSizeLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
