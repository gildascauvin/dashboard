import { RatioMovementPipe } from './ratio-movement.pipe';

describe('RatioMovementPipe', () => {
  it('create an instance', () => {
    const pipe = new RatioMovementPipe();
    expect(pipe).toBeTruthy();
  });
});
